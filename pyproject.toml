[project]
name = "tasker-contrib-tools"
version = "0.1.0"
description = "Development tools for tasker-contrib template validation"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
