"""Business logic services for the FastAPI example app.

Each module contains pure business logic extracted from step handlers.
Services take plain values and return plain dicts — no Tasker types required.
"""
